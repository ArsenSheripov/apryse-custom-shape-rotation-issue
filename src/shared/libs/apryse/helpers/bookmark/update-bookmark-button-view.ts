// import { store } from 'app/providers/redux-provider/store';

// import { getBookmarkSvg } from './get-bookmark-svg';

// export const updateBookmarkButtonView = (pageNumber: number) => {
//   const state = store.getState();
//   const { bookmarks } = state.viewer;

//   // eslint-disable-next-line no-prototype-builtins
//   const hasBookmark = bookmarks.hasOwnProperty(pageNumber);
//   const button = document.querySelector(`#bookmark-button-${pageNumber}`);

//   if (button) button.innerHTML = getBookmarkSvg(hasBookmark);
// };
